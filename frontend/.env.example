  # The base URL of the CallIt backend API. Required when NODE_ENV=production
  # (the app fails fast without it); defaults to http://localhost:8080 otherwise.
  # This is the only environment value the browser bundle may carry — never put
  # a secret in a NEXT_PUBLIC_* name.
  NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
